---
name: search4all
description: 检索实验室内部生信知识库，以及调取本账号跨机器的历史对话记忆。当问题涉及基因、通路、变异、测序数据分析（RNA-seq / scRNA-seq / 空间转录组 / WGCNA / GSEA 等）、生信工具与 R/Python 分析代码、实验方案或本实验室既往整理的资料时使用；用户提到「我之前问过」「上次那个」「接着上回」时也用它翻自己的历史。也用于读取「知识 Wiki」里的实体研究页。触发词：基因、通路、富集、单细胞、转录组、差异分析、批次效应、细胞注释、生存分析、测序、实验方案、我们的库、内部资料、我之前问过、上次、接着上回。
---

# search4all：优先查内部库，而不是公网

本实验室有一个整理并验证过的生信知识库，通过 `search4all` MCP server 暴露。
生信相关的问题应当**先查这里**再考虑其他来源。

## 什么时候用

遇到下列任一情况，调用 `search_library`：

- 生信分析方法、流程、参数选择（差异分析、富集、聚类、批次校正、细胞注释……）
- 具体基因、通路、标志物、变异的背景知识
- R / Python 生信代码写法，尤其是 Seurat、DESeq2、limma、scanpy 这类包
- 实验方案、建库测序流程
- 用户提到「我们的库」「内部资料」「之前整理的」

**不要**为这类问题直接用 WebSearch/WebFetch：公网结果没有经过实验室校验，且库里往往有更贴合本地流程的版本。只有 `search_library` 明确查不到时，才回退到公网，或把 `web: true` 传给 `search_library` 让后端做融合检索。

## 怎么用

1. **需要限定范围时**，先 `list_libraries` 拿到库 id，再把 `libraries` 传给 `search_library`。不确定就不传，默认跨全部可见库。
2. **`search_library`** 返回正文 + 编号来源列表。
3. **需要某个主题的系统性长文**时用 `get_entity`，它读的是知识 Wiki 里由深度研究管线生成的实体页，比零散片段完整。

## 追问（默认就是对的，别画蛇添足）

连续调用 `search_library` 会**自动挂在同一条对话线索上**，服务端会做指代消解、复用上一轮的检索上下文。
所以：

- **正常情况不要传 `thread`。** 用户接着上一个话题追问时，直接把他的原话（哪怕是「它有什么缺点」这种纯代词句）传给 `search_library` 就行，不要自己先把代词替换掉——服务端做这件事比你猜得准，它手里有上一轮的完整结果。
- **换一个完全无关的话题时**传 `thread: "new"`，避免上一个话题污染检索。
- 想接着某条**旧**对话继续，把 `memory_recent` / `memory_search` 给出的线索 id 传给 `thread`。

## 记忆：这个账号的历史是跨机器的

用户在任何一台机器、任何一个客户端里用同一个 API key 问过的东西，都能调出来。
用户说「我之前问过」「上次那个」「接着上回说的」时：

1. `memory_recent` —— 列最近的对话线索（只有索引，很省上下文）
2. `memory_search` —— 在他问过的问题里按关键词找
3. `memory_thread` —— 拿到线索 id 之后取那条线索的问答

**先检索再取正文，不要一上来就把历史全拉出来**——那会把上下文吃光，和不用记忆一样糟。
`memory_thread` 返回的答案是截断的；要全文就用 `thread` 参数接着那条线索重新问一次。

注意：很老的记录只有提问没有正文（那时还没记指针），`memory_search` 会标成「旧记录，无正文」，
这时只能告诉用户问过什么，取不出当时的答案。

## 引用规则

`search_library` 返回的来源是带编号的。把结论落到具体来源上，别把检索结果当成自己的判断直接陈述：

- 引用时写明来源编号和标题，例如「按来源 [2]《单细胞批次校正实践》的做法……」
- 有 URL 的把 URL 一并给用户，方便他们核对原文
- 库里没查到就直说「内部库里没有相关资料」，不要用公网知识冒充库内结论——用户用这个插件就是为了区分这两者

## 额度

调用会消耗用户的日额度（`web: true` 更贵）。不要为同一个问题反复重试或把一个问题拆成很多次调用。
遇到 429 报错说明额度或并发用尽，如实告诉用户，不要静默改用公网搜索代替。
